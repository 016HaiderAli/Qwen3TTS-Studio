# GPU worker dependencies (Qwen3-TTS real inference). Install on the CUDA GPU
# host together with requirements.txt. NOT installed in the dev environment.
#
# Version evidence: reference/Voice_Studio.ipynb cell 7 installed qwen-tts 0.1.1
# (the current release), which pins transformers==4.57.3 and accelerate==1.12.0.
# Cell 9 ran on PyTorch 2.11.0+cu128, Python 3.12, NVIDIA Tesla T4; torch itself
# is not pinned here because the CUDA wheel must match the host. flash-attn is
# OPTIONAL (the notebook ran without it, with a fallback warning).
#
# The path-form audio loading used by create_voice_clone_prompt goes through
# librosa, a transitive dependency of qwen-tts==0.1.1.
qwen-tts==0.1.1
# Pinned by qwen-tts==0.1.1 itself (do not change unless upgrading qwen-tts):
transformers==4.57.3
accelerate==1.12.0

torch>=2.4
soundfile>=0.12
numpy>=1.26
sentencepiece>=0.2
